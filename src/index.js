import React from 'react'
import ReactDOM from 'react-dom'
import App from './App'
import './index.css'
//Note: style.css is empty but project won't build without it
import './style.css'

ReactDOM.render(<App />, document.getElementById('root'))